# 性能测试报告

| 项目名称 | 资产经营管理系统 |
|----------|------------------|
| 文档版本 | V1.0（基线模板） |
| 报告日期 | 2026-08-26 |
| 测试状态 | **计划基线** — 正式报告在 UAT 前执行后更新 |

---

## 1. 测试目的

验证系统在 SRS 约束下满足 **200 同时在线用户** 场景，核心 API 响应与稳定性达标，为上线提供依据。

## 2. 测试依据

- SRS V2.2 §5.1 性能需求
- [测试计划.md](./测试计划.md)
- [概要设计说明书](../design/概要设计说明书.md)

## 3. 测试环境

| 项 | 配置（计划） |
|----|--------------|
| API 服务器 | 4C8G × 2 实例 |
| PostgreSQL | 4C16G，SSD |
| Redis | 2G |
| 网络 | 机房内网 / 模拟 100M |
| 工具 | k6 / JMeter |
| 数据量 | 资产 ≥1 万、合同 ≥5 千、账单 ≥10 万（造数） |

## 4. 测试场景

### 4.1 基准场景

| 场景 ID | 描述 | 并发 | 时长 |
|---------|------|------|------|
| PERF-001 | 混合登录 | 50 | 10min |
| PERF-002 | 资产列表查询 | 100 | 15min |
| PERF-003 | 收费大厅列表 | 80 | 15min |
| PERF-004 | 合同详情+账单 | 50 | 10min |

### 4.2 目标场景（200 在线）

| 场景 ID | 用户行为混合比 | 总虚拟用户 |
|---------|----------------|------------|
| PERF-005 | 登录 10%、查询 50%、写操作 20%、支付回调 5%、其他 15% | 200 |

**写操作包括**：创建报修、登记收款、审批提交（压测环境 Mock 审批）。

### 4.3 峰值场景

| 场景 ID | 描述 | 并发 |
|---------|------|------|
| PERF-006 | 月初集中出账后查询高峰 | 250（短时） |
| PERF-007 | 公开招租报名截止瞬时 | 150 |

## 5. 性能指标与通过标准

| 指标 | 目标 | 说明 |
|------|------|------|
| API P95 延迟 | ≤ 2000 ms | 查询类接口 |
| API P95 延迟 | ≤ 3000 ms | 写操作含事务 |
| 错误率 | < 1% | 5xx + 业务失败 |
| CPU（API） | < 70%  sustained | 200 并发时 |
| PG 连接 | < max_connections 80% | |
| 登录 TPS | ≥ 30 | |

## 6. 测试结果（待执行）

> 正式测试完成后填写本节并更新文档版本为「实测报告」。

### 6.1 汇总

| 场景 ID | P50(ms) | P95(ms) | P99(ms) | 错误率 | 结论 |
|---------|---------|---------|---------|--------|------|
| PERF-001 | — | — | — | — | 待测 |
| PERF-002 | — | — | — | — | 待测 |
| PERF-005 | — | — | — | — | 待测 |

### 6.2 瓶颈分析（待填）

- CPU / 内存 / IO / 慢查询 / 第三方依赖

### 6.3 优化措施（待填）

| 问题 | 措施 | 复测结果 |
|------|------|----------|
| — | — | — |

## 7. k6 脚本示例（PERF-005 片段）

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  stages: [
    { duration: '2m', target: 100 },
    { duration: '5m', target: 200 },
    { duration: '2m', target: 0 },
  ],
  thresholds: {
    http_req_failed: ['rate<0.01'],
    http_req_duration: ['p(95)<2000'],
  },
};

const BASE = __ENV.API_BASE || 'http://localhost:3000/api/v1';
const TOKEN = __ENV.TOKEN;

export default function () {
  const headers = {
    Authorization: `Bearer ${TOKEN}`,
    'X-Client-Type': 'admin',
  };
  const r = http.get(`${BASE}/assets?page=1&pageSize=10`, { headers });
  check(r, { 'status 200': (res) => res.status === 200 });
  sleep(1);
}
```

## 8. 结论与建议

| 状态 | 说明 |
|------|------|
| 当前 | 性能测试方案与指标已基线化，**待 STAGING 环境执行** |
| 上线前 | 必须完成 PERF-005 并满足 §5 指标 |
| 建议 | 生产启用只读副本承担报表；Redis 缓存经营看板指标 |

## 9. 签字

| 角色 | 姓名 | 日期 |
|------|------|------|
| 测试负责人 | | |
| 技术负责人 | | |
| 项目经理 | | |

---

**关联**：[测试计划.md](./测试计划.md)、[部署手册](../deployment/部署手册.md)
