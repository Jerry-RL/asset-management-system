# PC 管理后台 — 用户手册（模块导览）设计

## 目标

顶栏「帮助」进入独立页 `/help`，按侧栏菜单分组说明每个模块的**用途、主要能力、典型操作**，并可跳转到对应功能页。

## 方案

- 前端内置 `helpManual.ts`，结构对齐 `MENU`
- 不占侧栏菜单；`PageTabs` 显示「用户手册」
- 左目录 + 右详情；支持搜索；「前往模块」导航

## 非目标

- 不做分步培训长文、不做 CMS 下发、不渲染 `docs/user/用户手册.md`
